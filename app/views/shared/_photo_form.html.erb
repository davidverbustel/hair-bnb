<%= simple_form_for([@accomodation, Photo.new], html: { multipart: true }) do |f| %>
    <%= f.error_notification %>
    <%= f.input :picture %>
    <%= f.input :picture %>
    <%= f.input :picture %>
    <%= f.input :picture %>
    <%= f.button :submit %>
<% end %>
